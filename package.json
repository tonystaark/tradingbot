{
  "name": "tradingbot",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "dev": "pnpm -r --parallel dev",
    "dev:bot": "pnpm --filter bot dev",
    "dev:web": "pnpm --filter web dev"
  },
  "engines": {
    "node": ">=18",
    "pnpm": ">=8"
  }
}
